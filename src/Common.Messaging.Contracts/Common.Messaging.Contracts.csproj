<Project Sdk="Microsoft.NET.Sdk">

  <!-- Shared build/quality settings come from Directory.Build.props (solution root). -->
  <PropertyGroup>

    <!-- Public contract library: ship XML docs; keep the public surface deliberate. -->
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
  </PropertyGroup>

  <ItemGroup>
    <!-- The batcher (engine) sets a record's serialized-form memo when sizing it for the byte cap. -->
    <InternalsVisibleTo Include="Common.FileIngestion" />
    <InternalsVisibleTo Include="Common.Messaging.Contracts.Tests" />
  </ItemGroup>

</Project>
